# FPL Edge 2.3

Production build for Team 702959. Uses the official Fantasy Premier League public API. A GitHub Actions snapshot is refreshed every 15 minutes and served by the Vercel app, avoiding runtime blocks from the FPL endpoint.

Deploy from the repository root. No vercel.json is required for the Express app.
